#include<stdio.h>
#include<string.h>
int main()
{
	char a[100],b[100];
	int s,t;
	scanf("%s",a);
	scanf("%s",b);
	s=strlen(a);
	t=strlen(b);
	for(i=0;i<s;i++)
	{
		for(j=0;j<t;j++)
		{
			if(a[i]==a[j])
				i++;
			else
				for(i=0;i='\0';i++)

return 0;
}
